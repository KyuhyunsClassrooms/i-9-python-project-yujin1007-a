# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제: 온도조건과 반응시간에 따른 화학 반응 속도 및 수득률 계산기


---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.
시간의 흐름에 따라 변하는 농도 데이터를 사람이 손으로 직접 계산(반올림 등)할 경우, 누적되는 미세한 오차로 인해 최종 생성물의 정확한 수득률을 예측하기 어렵다. 특히 특정 농도 이하로 떨어지는 위험하거나 주의해야 할 타이밍을 정확한 수학적 경계선으로 포착해 내기가 쉽지 않다.


## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

- 초기농도
- 반응시간
- 온도조건
- 반응 속도 상수(온도 선택에 따라 자동으로 지정되는 고정된 값)

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- 속도 상수 결정: 온도 조건에 따라 반응 속도 상수를 다르게 설정 (저온: 0.05, 고온: 0.15)
-반응 시뮬레이션: while 반복문을 사용하여 10초 간격으로 감소하는 반응물 농도와 증가하는 생성물 농도를 화학 공식에 맞게 누적 계산
-안전 상태 진단: 현재 반응물 농도가 0.2 mol/L 미만으로 떨어지면 공정 상태를 "주의"로, 아니면 "정상"으로 판별
-수득률 계산: 최종 생성물 농도를 초기 농도로 나누어 백분율로 환산하고, 80%를 기준으로 우수/보완 필요 평가

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

- 시뮬레이션 과정 표 (시간이 지남에 따라 반응물은 줄어들고 생성물은 늘어나는 모습을 표 형태로 출력)
- 최종 결과 및 수득률
-공정 효율성 평가 메시지

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름: reaction_records

reaction_records = [
    # [시간(초), 반응물 농도, 생성물 농도]
    [0,  1.0,  0.0],   # 0초일 때 (초기 상태)
    [10, 0.8,  0.2],   # 10초일 때
    [20, 0.64, 0.36],  # 20초일 때
    [30, 0.51, 0.49]   # 30초일 때 (최종 상태 예시)
]




### 한 행의 의미
특정 시간 단위별 반응 상태 기록

### 각 열의 의미

| 열 번호 | 의미 | 예시 |
|---:|---|---|
| 0번 열 | 열반응이 진행된 시간  | 0, 10, 20, 30(초) |
| 1번 열 | 남아있는 반응물의 농도 | 1.0, 0.8, 0.64 (단위: mol/L)|
| 2번 열 | 새로 생성된 생성물의 농도 | 0.0, 0.2, 0.36(단위: mol/L) |
| 3번 열 | 현재 공정의 안전 상태 또는 단계별 평가 | "정상", "정상", "경고" |


## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |

| get_rate_constant(choice) | 사용자가 선택한 온도 조건에 따라 화학 반응 속도 상수를 결정하는 함수 | choice (사용자가 입력한 온도 번호: 1 또는 2) | rate (반응 속도 상수 값: 0.05 또는 0.15) |
| run_simulation(initial_conc, total_time, rate) | 입력받은 조건으로 시간 흐름에 따른 농도 변화를 계산하고 2차원 리스트에 저장하는 핵심 함수 | initial_conc(초기 농도), total_time(총 반응 시간), rate(반응 속도 상수) | reaction_records (데이터가 쌓인 완성된 2차원 리스트) |
| print_result_report(records, initial_conc) | 저장된 2차원 리스트 데이터를 화면에 표 형태로 예쁘게 출력하고, 최종 수득률을 계산하여 공정을 평가하는 함수 | records(데이터가 담긴 2차원 리스트), initial_conc(초기 농도) | 없음 (print()로 화면 출력만 수행) | 



---

## 6. 사용할 제어구조

### 조건문을 사용할 부분

예) 사용자의 예산보다 가격이 낮은 항목만 추천한다.

내 계획:
-사용자가 선택한 온도 조건 번호(1번: 저온, 2번: 고온)를 if-elif-else문으로 판단하여 프로그램 내부에서 사용할 서로 다른 '반응 속도 상수' 값을 지정한다.

-시뮬레이션 과정 중 특정 기준(예: 반응물 농도가 0.2 mol/L 미만으로 떨어지는 상황 등)이 되면 공정 상태를 "정상"에서 "주의" 또는 "경고"로 변경하여 2차원 리스트에 저장한다.

-최종 계산된 수득률(%)이 80% 이상이면 "[우수] 공정", 미만이면 "[보완 필요] 공정"으로 판정하여 서로 다른 평가 메시지를 출력한다.

### 반복문을 사용할 부분

예) 2차원 리스트의 모든 항목을 하나씩 검사한다.

내 계획:
-사용자가 입력한 '총 반응 시간'까지 지정한 시간 간격으로 시간이 흐르도록 while 반복문(또는 range를 활용한 for문)을 수행하며, 매 시간마다 줄어드는 반응물과 늘어나는 생성물 농도를 계산한다.

-시뮬레이션이 끝난 후, 데이터가 누적된 2차원 리스트(reaction_records)의 행을 for row in reaction_records: 반복문으로 하나씩 꺼내어 화면에 깔끔한 표 형태로 출력한다.

---

## 7. 예상되는 예외 상황


1. 잘못된 메뉴 및 음수 입력: 사용자가 온도 조건을 고를 때 1, 2 이외의 문자나 정수를 입력하거나, 초기 농도 및 반응 시간에 0 이하의 음수를 입력하는 경우
2. 반응이 너무 빨리 끝나는 경우: 사용자가 반응 시간을 너무 길게 주거나 고온을 선택해서 시뮬레이션 도중에 반응물 농도가 0 이하(마이너스)로 떨어지는 경우

---

## 8. Gemini에게 도움받고 싶은 부분



1. 2차원 리스트 데이터 처리: 출력 함수에서 보기 좋게 정렬하여 출력하는 문법 도움받기.
2. 핵심 알고리즘 구현 도움
3. 코드를 직접 작성하는 과정에서 발생하는 에러의 원인을 분석하고, 스스로 고칠 수 있는 힌트와 최소한의 수정 코드 조각 지원받기.
